LocaleProvider from @casualoffice/docs. Use via `window.CasualOfficeDocs.LocaleProvider` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface LocaleProviderProps {
  i18n?: unknown;
  children: React.ReactNode;
}
```
