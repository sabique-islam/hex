ParseErrorDisplay from @casualoffice/docs. Use via `window.CasualOfficeDocs.ParseErrorDisplay` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface ParseErrorDisplayProps {
  message: string;
  details?: string;
  onRetry?: () => void;
  className?: string;
}
```
