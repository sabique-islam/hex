TableInsertButtons from @casualoffice/docs. Use via `window.CasualOfficeDocs.TableInsertButtons` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface TableInsertButtonsProps {
  onAction: (action: TableAction) => void;
  disabled?: boolean;
}
```
