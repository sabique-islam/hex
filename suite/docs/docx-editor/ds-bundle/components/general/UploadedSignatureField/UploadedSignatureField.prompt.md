UploadedSignatureField from @casualoffice/docs. Use via `window.CasualOfficeDocs.UploadedSignatureField` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface UploadedSignatureFieldProps {
  onCapture: (sig: CapturedSignature) => void;
  /** Accept attribute. Default image/*. */
  accept?: string;
}
```
