TypedSignatureField from @casualoffice/docs. Use via `window.CasualOfficeDocs.TypedSignatureField` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface TypedSignatureFieldProps {
  onCapture: (sig: CapturedSignature) => void;
  defaultText?: string;
  saveLabel?: string;
}
```
