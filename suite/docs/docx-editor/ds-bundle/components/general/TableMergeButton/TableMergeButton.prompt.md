TableMergeButton from @casualoffice/docs. Use via `window.CasualOfficeDocs.TableMergeButton` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface TableMergeButtonProps {
  onAction: (action: TableAction) => void;
  disabled?: boolean;
  canMerge?: boolean;
  canSplit?: boolean;
}
```
