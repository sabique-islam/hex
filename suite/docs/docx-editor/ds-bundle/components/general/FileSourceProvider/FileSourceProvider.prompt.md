FileSourceProvider from @casualoffice/docs. Use via `window.CasualOfficeDocs.FileSourceProvider` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface FileSourceProviderProps {
  source: FileSource;
  children: React.ReactNode;
}
```
