ResponsiveToolbarGroup from @casualoffice/docs. Use via `window.CasualOfficeDocs.ResponsiveToolbarGroup` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface ResponsiveToolbarGroupProps {
  /** Group items */
  children: React.ReactNode;
  /** Gap between items */
  gap?: number;
  /** Whether to show separator after group */
  separatorAfter?: boolean;
  /** Additional className */
  className?: string;
  /** Additional styles */
  style?: React__default.CSSProperties;
}
```
