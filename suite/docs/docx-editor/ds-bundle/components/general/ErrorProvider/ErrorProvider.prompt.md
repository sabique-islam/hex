ErrorProvider from @casualoffice/docs. Use via `window.CasualOfficeDocs.ErrorProvider` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface ErrorProviderProps {
  children: React.ReactNode;
}
```
