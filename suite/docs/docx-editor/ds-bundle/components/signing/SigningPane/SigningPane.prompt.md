SigningPane from @casualoffice/docs. Use via `window.CasualOfficeDocs.SigningPane` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface SigningPaneProps {
  /** Optional banner override; falls back to session.banner. */
  banner?: string;
  /** Optional data-testid root. */
  testId?: string;
}
```
