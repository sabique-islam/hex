---
'@casualoffice/docs': minor
---

The numbered vertical (left-margin) ruler is now off by default — it read as clutter next to the document, and Google Docs shows only the horizontal ruler. The horizontal ruler is unchanged. A new `showVerticalRuler` prop (default `false`) and a "Vertical ruler" toggle under the View menu bring it back for anyone who wants it.
