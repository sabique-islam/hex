---
'@casualoffice/docs': patch
---

Chrome polish: the Find bar no longer overlaps the toolbar (it measures the toolbar and docks clearly below it, on a proper tokenized surface); the Comments sidebar shows a designed empty state ("No comments yet") when opened with no comments instead of rendering nothing; and the Insert Table and Page Setup dialogs now use the shared Dialog shell (blurred scrim, unified header/footer, focus trap) so they match the rest of the dialog family.
