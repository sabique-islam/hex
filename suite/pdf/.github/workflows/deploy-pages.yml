name: Deploy (GitHub Pages)

# Auto-deploys to pdf.casualoffice.org on push to main.
# Prereqs (done 2026-06-27): Pages Source = GitHub Actions; repo variable
# PAGES_CUSTOM_DOMAIN = pdf.casualoffice.org (writes CNAME below); DNS CNAME ->
# casualoffice.github.io with Enforce HTTPS.
on:
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive # vendor/design-system (link: override) must be present for install
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm -C apps/web run build
      - name: Apply custom domain (if configured)
        run: |
          if [ -n "${{ vars.PAGES_CUSTOM_DOMAIN }}" ]; then
            echo "${{ vars.PAGES_CUSTOM_DOMAIN }}" > apps/web/dist/CNAME
            echo "CNAME set to ${{ vars.PAGES_CUSTOM_DOMAIN }}"
          fi
      - uses: actions/upload-pages-artifact@v3
        with:
          path: apps/web/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url || steps.retry1.outputs.page_url || steps.retry2.outputs.page_url }}
    steps:
      # actions/deploy-pages intermittently hits a transient Pages backend error
      # ("Deployment failed, try again later.") that fails within seconds of the
      # deployment API call, unrelated to the build. The blip can last longer than
      # the gap between back-to-back retries, so retry with BACKOFF: up to 3
      # attempts with 45s / 90s sleeps between them, giving the backend time to
      # recover. Dependency-free (no third-party retry action) on purpose.
      - id: deployment
        uses: actions/deploy-pages@v4
        continue-on-error: true
      - name: Backoff before retry 1
        if: steps.deployment.outcome == 'failure'
        run: sleep 45
      - name: Retry deployment (attempt 2)
        id: retry1
        if: steps.deployment.outcome == 'failure'
        uses: actions/deploy-pages@v4
        continue-on-error: true
      - name: Backoff before retry 2
        if: steps.deployment.outcome == 'failure' && steps.retry1.outcome == 'failure'
        run: sleep 90
      - name: Retry deployment (attempt 3, final)
        id: retry2
        if: steps.deployment.outcome == 'failure' && steps.retry1.outcome == 'failure'
        uses: actions/deploy-pages@v4
